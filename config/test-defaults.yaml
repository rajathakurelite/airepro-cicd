# All Playwright tests live in airepro_cicd — every test job clones this repo
test_repo:
  git_url: https://github.com/eliteprofessional/airepro_cicd.git
  branch: fixes_ai
  credentials_id: github-rajathakur
  install_script: scripts/ci-setup-node.sh
  run_script: scripts/run-service-tests.js
  default_env: stage

# Deploy repos use this credential unless overridden per service
deploy:
  credentials_id: github-rajathakur

# Jenkins infra repo (this repo) — for pipeline script from SCM
infra_repo:
  git_url: https://github.com/rajathakurelite/airepro-cicd.git
  branch: main
  credentials_id: github-rajathakur
